synthetic: true
label: "Synthetic switch and diode loss example (round numbers, not a real design)"
label_ko: "합성 스위치·다이오드 손실 예제 (단순한 어림수이며 실제 설계가 아님)"
params:
  C_node: 1.0e-10
  V_sw: 48
  f_s: 1.0e5
  Q_g: 2.0e-8
  V_GS: 10
  V_F: 0.5
  I_avg: 1
  r_d: 0.05
  I_rms: 1.2
steps:
  - {eq: loss.sw.cap, as: P_sw}
  - {eq: loss.gate, as: P_gate}
  - {eq: loss.diode, as: P_diode}
