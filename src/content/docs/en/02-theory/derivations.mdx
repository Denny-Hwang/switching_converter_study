---
title: Derivations
description: Every derived equation, re-derived step by step with sympy. The steps are exported from the same scripts that pytest runs against equations.yaml.
sidebar:
  order: 90
---

import Derivation from '../../../../components/Derivation.astro';

Each section below is generated from a script in `python/pe_core/derive/`.
The same script runs under `pytest`, which fails unless the symbolic
difference between the derived result and the expression stored in
`equations.yaml` simplifies to zero. The prose and headings are written by
hand; every formula on this page is printed by sympy from those scripts.

## Refreshers: element laws, impedance, Fourier series, magnetics, the ideal transformer

<Derivation module="foundations" />

## Averaging, balance, and losses in the averaged model

<Derivation module="averaging" />

## CCM conversion ratios and ripple

<Derivation module="ccm_ratios" />

## CCM/DCM boundary and DCM conversion ratios

<Derivation module="dcm" />

## Flyback: DCM ratio, boundaries, stresses, loss-free resistor

<Derivation module="flyback" />

## Flyback clamp: reflected voltage, clamp dissipation, output ceiling

<Derivation module="clamp" />

## Snubbers: parasitics from the ringing, damping resistance and loss

<Derivation module="snubbers" />

## Topology details: dc currents, ripple, switch stress, utilization

<Derivation module="topologies" />

## Small-signal transfer functions, CCM

<Derivation module="small_signal" />

## PWM modulator and loop gain

<Derivation module="control" />

## Stored energy, losses and ringing

<Derivation module="energy" />

## Magnetic circuits: the inductance factor, flux density and the core geometrical constant

<Derivation module="magnetics" />

## Windings: resistance, skin depth, thin layers, leakage and the short circuit

<Derivation module="windings" />

## Measurement: inductance read near self-resonance

<Derivation module="measurement" />

## Linear sources: maximum power and constant-voltage extraction

<Derivation module="harvesting" />

## Current-output shunt monitor

<Derivation module="sensing" />

## Bench: edges, gate drive, rise time, inrush, heat and a battery's terminals

<Derivation module="bench" />
