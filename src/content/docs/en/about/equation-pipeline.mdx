---
title: Equation pipeline check
description: One equation, rendered from the single source of truth, with its verification badge — and the same engine running in the browser.
sidebar:
  order: 2
---

import Eq from '../../../../components/Eq.astro';
import EqRef from '../../../../components/EqRef.astro';
import PipelinePlot from '../../../../tools/PipelinePlot.tsx';

This page exercises the chain that every later page depends on:

1. `packages/pe-core/equations/equations.yaml` holds the expression, its
   assumptions, its citation and hand-checked test values.
2. `python scripts/gen_equations.py` parses it with sympy, generates the
   LaTeX and the shared test vectors, and fails if a test value disagrees
   with the expression.
3. The TypeScript engine is tested against those vectors (1e-9 relative),
   and every generated formula is rendered by KaTeX in strict mode.
4. The docs embed the result with `<Eq id="…" />`; nobody types LaTeX.

<Eq id="buck.ccm.M" />

Open the expander under the equation for its assumptions and source; the
badge links to the parity test. This page shows the equation only to
demonstrate the pipeline; it is explained on the
<EqRef id="buck.ccm.M" label="volt-second balance page" />.

## The same engine in the browser

The chart below is a React island that calls the TypeScript evaluators of
the buck and boost CCM conversion ratios in your browser and plots them with
Plotly. The [equation explorer](../../design/explorer/) does the same for any
equation in the catalogue.

<PipelinePlot client:only="react" label="Conversion ratio versus duty ratio of the ideal buck and boost converters in CCM" xTitle="D" />
