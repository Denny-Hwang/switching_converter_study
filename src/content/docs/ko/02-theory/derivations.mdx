---
title: 유도 과정
description: 유도된 모든 수식을 sympy로 단계별로 다시 유도합니다. 각 단계는 pytest가 equations.yaml과 대조하는 바로 그 스크립트에서 내보낸 것입니다.
sidebar:
  order: 90
---

import Derivation from '../../../../components/Derivation.astro';

아래 각 절은 `python/pe_core/derive/`의 스크립트에서 생성됩니다. 같은
스크립트가 `pytest`에서도 실행되며, 유도 결과가 `equations.yaml`에 저장된
수식과 정확히 같게 정리되지 않으면(기호적 차이가 0이 아니면) 테스트가
실패합니다. 설명과 제목은 사람이 쓴 것이고, 이 페이지의 모든 수식은 그
스크립트에서 sympy가 출력한 것입니다.

## CCM 변환비와 리플

<Derivation module="ccm_ratios" />

## CCM/DCM 경계와 DCM 변환비

<Derivation module="dcm" />

## 플라이백: DCM 변환비, 경계, 스트레스, 무손실 저항

<Derivation module="flyback" />

## 저장 에너지, 손실, 링잉

<Derivation module="energy" />

## 자기 회로

<Derivation module="magnetics" />

## 선형 전원: 최대 전력과 정전압 추출

<Derivation module="harvesting" />

## 전류 출력형 션트 모니터

<Derivation module="sensing" />
