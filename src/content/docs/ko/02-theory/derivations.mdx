---
title: 유도 과정
description: 유도된 모든 수식을 sympy로 단계별로 다시 유도합니다. 각 단계는 pytest가 equations.yaml과 대조하는 바로 그 스크립트에서 내보낸 것입니다.
sidebar:
  order: 90
---

import Derivation from '../../../../components/Derivation.astro';

아래 각 절은 `python/pe_core/derive/`의 스크립트에서 생성됩니다. 같은
스크립트가 `pytest`에서도 실행되며, 유도 결과(derived result)와 `equations.yaml`에
저장된 수식의 기호적 차이(symbolic difference)가 0으로 정리되지 않으면 테스트가
실패합니다. 설명과 제목은 사람이 쓴 것이고, 이 페이지의 모든 수식은 그
스크립트에서 sympy가 출력한 것입니다.

## 기초 복습: 소자 법칙(element laws), 임피던스, 푸리에 급수, 자기학(magnetics), 이상적 변압기

<Derivation module="foundations" />

## 평균화(averaging), 평형 조건(balance), 평균 모델의 손실

<Derivation module="averaging" />

## CCM 변환비(conversion ratio)와 리플

<Derivation module="ccm_ratios" />

## CCM/DCM 경계(boundary)와 DCM 변환비

<Derivation module="dcm" />

## 플라이백: DCM 변환비, 경계, 스트레스, 무손실 저항(loss-free resistor)

<Derivation module="flyback" />

## 플라이백 클램프: 반사 전압(reflected voltage), 클램프 손실, 출력 상한

<Derivation module="clamp" />

## 스너버(snubber): 링잉에서 구한 기생 성분, 감쇠 저항, 손실

<Derivation module="snubbers" />

## 토폴로지 세부: 직류 전류, 리플, 스위치 스트레스, 이용률(utilization)

<Derivation module="topologies" />

## 소신호 전달함수(small-signal transfer functions), CCM

<Derivation module="small_signal" />

## PWM 변조기(modulator)와 루프 이득(loop gain)

<Derivation module="control" />

## 저장 에너지, 손실, 링잉

<Derivation module="energy" />

## 자기 회로(magnetic circuits): A_L, 자속 밀도, 코어 기하 상수

<Derivation module="magnetics" />

## 권선(windings): 저항, 표피 깊이(skin depth), 얇은 층, 누설(leakage), 단락

<Derivation module="windings" />

## 측정: 자기 공진 근처에서 읽히는 인덕턴스

<Derivation module="measurement" />

## 선형 전원(linear sources): 최대 전력과 정전압 추출(constant-voltage extraction)

<Derivation module="harvesting" />

## 전류 출력형 션트 모니터

<Derivation module="sensing" />
