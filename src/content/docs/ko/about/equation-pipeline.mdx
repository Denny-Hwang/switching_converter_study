---
title: 수식 파이프라인 점검
description: 단일 원본에서 렌더링된 수식 하나와 검증 배지 — 그리고 브라우저에서 동작하는 같은 엔진.
sidebar:
  order: 2
---

import Eq from '../../../../components/Eq.astro';
import PipelinePlot from '../../../../tools/PipelinePlot.tsx';

이 페이지는 이후 모든 페이지가 의존하는 검증 체인을 실제로 거쳐 봅니다.

1. `packages/pe-core/equations/equations.yaml`에 수식, 가정, 인용, 손으로
   확인한 테스트 값이 들어 있습니다.
2. `python scripts/gen_equations.py`가 sympy로 이를 파싱하여 LaTeX와 공유
   테스트 벡터를 생성하며, 테스트 값이 수식과 다르면 실패합니다.
3. TypeScript 엔진은 이 벡터에 대해 상대오차 1e-9로 테스트되고, 생성된
   모든 수식은 KaTeX 엄격 모드로 렌더링됩니다.
4. 문서는 `<Eq id="…" />`로 결과를 삽입합니다. 누구도 LaTeX를 직접 입력하지
   않습니다.

<Eq id="buck.ccm.M" />

수식 아래의 펼침 메뉴에서 가정과 출처를 확인할 수 있으며, 배지는 패리티
테스트로 연결됩니다.

## 브라우저에서 동작하는 같은 엔진

아래 차트는 현재 카탈로그에 있는 두 수식의 TypeScript 평가 함수를 호출해
Plotly로 그리는 React 아일랜드입니다.

<PipelinePlot client:only="react" label="카탈로그의 두 수식에 대한 듀티비 대비 변환비" xTitle="D" />
