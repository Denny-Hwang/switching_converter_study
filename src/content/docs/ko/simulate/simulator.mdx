---
title: 컨버터 시뮬레이터
description: 벅, 부스트, 벅-부스트, 플라이백, 포워드 컨버터를 브라우저에서 시뮬레이션하고 그 정상상태를 수식과 비교하는 구간별 선형(piecewise-linear) 시간 영역 시뮬레이터.
sidebar:
  order: 1
---

import { Image } from 'astro:assets';
import Simulator from '../../../../components/Simulator.astro';
import Cite from '../../../../components/Cite.astro';
import EqRef from '../../../../components/EqRef.astro';
import Worked from '../../../../components/Worked.astro';
import Val from '../../../../components/Val.astro';
import TrySim from '../../../../components/TrySim.astro';
import shot from '../../../../assets/screenshots/simulator-ko.png';

토폴로지(topology)나 합성(synthetic) 프리셋을 고르고 파라미터를 SI 단위로
입력하십시오. 값을 직접 입력하거나 슬라이더를 움직이면 되며, 슬라이더는 현재
값의 위아래로 한 디케이드씩을 움직입니다(듀티비의 슬라이더는 0에서 1까지).
그러면 시뮬레이터가 주기적 정상상태(periodic steady state)의 한 주기를
계산하여 다음을 보여 줍니다.

- 파형: 인덕터 전류와 전압, 다이오드 전류, 스위치 전압, 출력 전압
- 검출한 전도 모드(conduction mode)와 $K$, $K_\mathrm{crit}$
- 같은 동작점(operating point)에서 시뮬레이션한 값과 카탈로그 수식의 값
- 파형으로 계산한 손실과 효율(efficiency)

모든 계산은 테스트 스위트가 검증하는 것과 같은 `pe-core` 엔진으로 브라우저
안에서 실행됩니다. URL에 전체 상태가 담기며, 학습 페이지의 "직접 해 보기"
링크는 프리셋을 채운 채로 시뮬레이터를 엽니다.

<Simulator />

## 모델링 범위

각 컨버터는 스위칭 주기의 구간마다 하나씩인 선형 회로의 집합입니다. 스위치 온
구간, 스위치가 꺼지고 다이오드가 도통하는 구간, 그리고 DCM에서는 휴지
구간(idle interval)이 있습니다 <Cite key="erickson2020" where="Ch. 2" />,
<Cite key="erickson2020" where="Ch. 5" />.

- **스위치와 다이오드.** 트랜지스터는 온 저항(on-resistance)을 선택적으로 가질
  수 있고 이상적인 바디 다이오드(body diode)를 가진 이상적인 스위치입니다.
  다이오드는 일정한 순방향 전압 강하를 가지며 이상적으로 차단하고,
  역회복(reverse recovery)은 없습니다. 인덕터에는 권선 저항을 줄 수 있습니다.
  다이오드를 거꾸로 흐르려는 전류는 흐르지 않습니다. 포워드 컨버터의 정류
  다이오드는 출력이 $n V_g$보다 높은 동안 차단하며, 턴오프 순간에 음수인 인덕터
  전류는 스위치의 바디 다이오드를 통해 계속 흐릅니다.
- **변압기.** 자화 인덕턴스(magnetizing inductance)를 제외하면 이상적입니다
  (1:n, $n$은 2차 턴 수를 1차 턴 수로 나눈 값). 누설 인덕턴스는 없습니다.
- **천이.** 스위치 양단에 커패시턴스가 없으면 천이는 순간적입니다. 커패시턴스를
  두면 턴오프 뒤에 인덕터 전류가 다이오드가 도통할 때까지 그 커패시턴스를
  충전합니다. 다이오드가 도통하는 동안 커패시턴스는 출력과 입력의 리플에 따라
  움직이는 스위치 전압을 따라가므로, 그 커패시터들의 전류 중 자기 몫을
  가져갑니다. 다이오드 전류가 0에 도달하면 인덕턴스가 커패시턴스와 함께
  링잉(ringing)합니다(<EqRef id="dcm.ring.f" label="링잉 주파수" />).
  스위치의 바디 다이오드는 스위치 전압 0에서 링잉을 클램프하며, 커패시턴스에
  저장된 에너지는 턴온할 때마다 손실됩니다(<EqRef id="loss.sw.cap" label="용량성 턴온 손실" />).
  따라서 커패시턴스가 있어도 입력 에너지는 출력 에너지와 손실의 합과 같습니다.
- **전원 구동 모드.** 입력을 고정 전압 대신 버스 커패시터를 충전하는 테브난
  전원(Thevenin source)으로 둘 수 있습니다. 출력과 듀티비(duty ratio)가
  고정되어 있으면, 이 모드는 플라이백의 정전압 싱크(constant-voltage sink)
  동작을 재현합니다(<EqRef id="flyback.V_crit" label="한 값에 묶이는 입력 전압" />).

## 계산 방법

- **한 구간 안에서** 상태(인덕터 전류, 커패시터 전압)는 선형 미분방정식을
  따릅니다. 시뮬레이터는 스위칭 주기의 2000분의 1인 하위 스텝(sub-step)마다 그
  방정식의 정확한 해(행렬 지수 함수)로 상태를 진행시킵니다. 스위치 양단
  커패시턴스의 링잉 한 주기가 하위 스텝 20개 이상에 걸치도록, 필요하면 스텝을
  더 짧게 합니다.
- 다이오드 전류가 0에 도달하거나 클램프가 도통하기 시작하는 것 같은
  **이벤트**(event)는 같은 정확한 해 위에서, 이분법(bisection)처럼 이벤트를 구간
  안에 가두는 탐색으로 하위 스텝 안에서 위치를 찾습니다(이분법보다 훨씬 적은
  단계로 끝나는 일리노이(Illinois) 변형의 가위치법(regula falsi)). 게이트 에지는
  주기의 시작과 듀티비 지점에 정확히 놓입니다.
- **반올림 오차.** 한 주기 안에서 시뮬레이터는 각 상태가 주기 시작 값에서
  벗어난 편차를 따라갑니다. 그러면 반올림 오차가 상태의 크기가 아니라 상태가
  움직인 양에 비례하므로, 아주 큰 커패시터라도 한 주기 동안의 변화량이 반올림
  수준까지 정확합니다.
- **주기적 정상상태**는 한 주기의 초기 상태를 다음 주기의 초기 상태로 보내는
  사상에 대한 뉴턴 반복(Newton iteration)으로 찾으며, 이때 사상의 정확한
  도함수를 씁니다. 이 도함수는 하위 스텝 행렬들의 곱이며, 시각이 상태에 따라
  달라지는 이벤트마다 보정됩니다. 최대 2000주기 안에서 모든 상태가 다음 두
  검사를 통과하면 수렴으로 판정합니다.
  - 한 주기 동안의 변화량이 그 상태가 주기 안에서 움직인 양의 백만분의 1보다
    작아야 합니다. 상태의 최댓값을 기준으로 삼으면 매 주기 같은 양씩 커지는
    전류도 충분히 커진 뒤에는 수렴으로 판정되므로 그렇게 하지 않습니다.
  - 남은 뉴턴 스텝, 즉 정상상태까지의 추정 거리가 상태 크기의 백만분의 1보다
    작아야 합니다. 변화량만으로는 부족합니다. 아주 큰 커패시터는 정상상태에서
    아직 멀리 떨어져 있어도 주기마다 조금밖에 변하지 않기 때문입니다.
- **모드**는 인덕터 전류가 주기의 일부 동안 0에 머물면 DCM, 0에 닿기만 하면
  BCM, 그 밖에는 CCM입니다. 전류가 거꾸로 흘러 스위치의 바디 다이오드로 계속
  흐르는 경우도 CCM입니다.

## 검증

테스트 스위트(`packages/pe-core/test/sim.test.ts`)는 시뮬레이터를 수식과
비교하여 검사합니다.

- **검증 격자(validation grid).** 벅, 부스트, 벅-부스트, 플라이백에 대해,
  듀티비와 $K_\mathrm{crit}$ 양쪽에 걸친 $K$ 값의 격자에서 다음을 확인합니다.
  - 시뮬레이션한 변환비(conversion ratio)와 인덕터 리플의 피크-피크 값이 수식과
    2 % 이내로 일치합니다.
  - 검출한 모드가 $K$와 $K_\mathrm{crit}$의 비교 결과와 일치합니다.
- **에너지 평형.** 손실이 있을 때 주기당 입력 에너지는 출력 에너지와 계산한
  손실의 합과 같습니다. 스위치 양단 커패시턴스가 있고 리플이 수십 볼트인
  경우에도 마찬가지입니다.
- **뉴턴 반복의 도함수.** 주기 사상의 정확한 도함수가 중심 유한 차분(central
  finite difference)과 일치합니다.
- **아주 큰 커패시터.** 시정수가 수십억 주기에 이르는 버스 커패시터도 무손실
  저항 분압 값에 자리 잡고, 아주 큰 출력 커패시터도 훨씬 작은 커패시터와 같은
  출력 전압을 냅니다.
- **링잉.** DCM의 링잉 주파수가 L-C 수식과 일치합니다.
- **전원 구동 플라이백.** 전원이 임계 전압에서의 DCM 전력보다 많은 전력을 공급할
  수 있으면 입력을 임계 전압에 묶어 두고, 그렇지 못하면 DCM에서 그보다 낮은
  전압에 자리 잡습니다.

## 풀이 예제: 전원을 한 값에 묶어 두는 플라이백

고정 듀티비의 합성 플라이백이 전류 제한 전원으로부터 고정 출력을 충전합니다.

<Worked example="flyback-source" show={['V', 'V_D', 'D', 'n', 'f_s', 'L_M', 'V_oc', 'R_s', 'V_gcrit', 'P_max', 'eta_ext', 'P_dcm']} />

입력은 CCM이 시작되는 <Val example="flyback-source" name="V_gcrit" />에 자리
잡습니다. 이 전압에서 전원은 가용 최대 전력 <Val example="flyback-source" name="P_max" />의
<Val example="flyback-source" name="eta_ext" />만큼을 공급할 수 있으며, 이는
컨버터가 DCM에서 끌어갈 <Val example="flyback-source" name="P_dcm" />보다
많습니다. 따라서 자화 전류가 커지다가 CCM이 입력을 임계 전압에 묶어 둡니다.
프리셋을 열고 시뮬레이션한 입력 전압을 수식과 비교하십시오(결과는 이 페이지
끝의 [스크린샷](#스크린샷)에 있습니다).

<TrySim example="flyback-source" topology="flyback" />

## 한계

- **천이 파형이 없습니다.** 누설 인덕턴스, 역회복, 스위칭 천이가 없으므로
  턴오프 스파이크나 겹침 손실(overlap loss)이 나타나지 않습니다.
- **듀티비가 고정입니다.** 제어 루프와 코어 손실이 없습니다.
- **주기적 정상상태가 존재해야 합니다.** 어떤 회로는 저항이 없으면 정상상태가
  없습니다. 고정 입력에서 CCM으로 고정 출력을 구동하는 경우, 출력이 입력보다
  높게 고정된 벅(전류가 바디 다이오드를 통해 거꾸로 흐름), 리셋
  한계(<EqRef id="forward.reset.Dmax" label="최대 듀티비" />)를 넘어 자화 전류가
  주기마다 계속 커지는 포워드 컨버터가 그렇습니다. 이들의 전류는 한없이 커지며,
  상태 표시줄은 정상상태에 도달하지 못했다고 알립니다. 온 저항이나 권선 저항이
  있으면 이들도 정상상태에 이르지만, 그 전류는 저항만이 제한하므로 매우 클 수
  있습니다.

## 스크린샷

<figure class="pe-shot">
  <Image src={shot} alt="풀이 예제의 프리셋으로 연 시뮬레이터: 상태 표시줄은 CCM을 보여 주고, 비교 패널은 시뮬레이션한 버스 전압을 V_g,crit 옆에 놓으며, 그래프는 한 주기 동안의 전류, 인덕터 전압, 스위치 전압, 출력 전압, 버스 전압을 보여 줍니다." />
  <figcaption>풀이 예제의 프리셋으로 연 시뮬레이터.</figcaption>
</figure>
